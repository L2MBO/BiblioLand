<div align="center">

# 📚 <a href="https://github.com/L2MBO/BiblioLand">BiblioLand</a>

**Локальное WinForms-приложение для удобного чтения и управления электронной библиотекой — без необходимости в подключении к интернету, с закладками, отзывами и многоуровневой модерацией.**

> *Разработано как дипломный проект для демонстрации навыков проектирования и реализации сложных ПО-решений на платформе .NET*

📄 **Полная документация проекта** — в файле [Пояснительная Записка.docx](Biblio/Docs/Documentations/Пояснительная_записка.docx)

📥 **Скачать релизы**: [Releases](https://github.com/L2MBO/BiblioLand/releases)

</div>

> [!NOTE]
>  
> **BiblioLand — это не просто читалка.**  
> Это **универсальная платформа**, которую можно использовать:
> - **лично** — как менеджер PDF-книг с закладками и статистикой,  
> - **в библиотеках** — для предоставления книг пользователям,  
> - **в учебных заведениях** — для доступа к учебной литературе,  
> - **в корпоративной среде** — для внутреннего документооборота и обучения.

> [!IMPORTANT]
> **Как зайти под суперадминистратором**
>
> В **пользовательской версии (`BiblioLand_v1.0.2`)** уже создан аккаунт суперадминистратора:
> - **Никнейм:** `SuperAdmin`
> - **Пароль:** `SuperAdmin!1`
>
> Вы можете изменить его данные в настройках профиля после входа.
>
> В **версиях для разработчиков** вы можете сами назначить роль суперадминистратора.  
> Чтобы назначить её, выполните SQL-запрос к базе данных:
> ```sql
> UPDATE Users 
> SET UserRoleID = 3 
> WHERE Username = 'ВашНикнейм';
> ```
> (Роль `3` = SuperAdmin, согласно таблице `UserRoles`)

> [!WARNING]
> **Восстановление пароля**
> 
> Функция восстановления пароля через отправку кода на email **не будет работать**, так как SMTP-сервер, использовавшийся для отправки писем, был остановлен и удалён.
> 
> Если вы хотите протестировать или использовать **рабочее восстановление пароля**, вам необходимо:
> - Настроить собственный SMTP-сервер (например, через Gmail, Mail.ru или локальный Postfix).
> - Указать корректные параметры подключения (хост, порт, логин, пароль) в файле `App.config` или в коде класса отправки писем.
> 
> Без подключения к SMTP-серверу приложение будет работать в **полностью автономном режиме**.

---

## 🛠️ Технологии

- **Язык**: C# (.NET Framework 4.7.2)  
- **UI-фреймворк**: Windows Forms + [Guna UI2](https://gunaui.com) + [MaterialSkin](https://github.com/IgnaceMaes/MaterialSkin)  
- **База данных**: MS SQL Server (локальная)  
- **ORM**: Entity Framework Core  
- **Безопасность**: Хеширование паролей через [BCrypt.Net-Next](https://github.com/BcryptNet/bcrypt.net)  
- **Графика**: [SixLabors.ImageSharp](https://github.com/SixLabors/ImageSharp) — для динамического размытия обложек на фоне  
- **Формат книг**: PDF (локальное хранение)

---

## 🌟 Основные возможности

### 👤 Для пользователей:
- Поиск и фильтрация книг по **названию, автору, жанру**
- **5 категорий закладок**: *читаю*, *буду читать*, *прочитано*, *брошено*, *отложено*
- **Оценка (1–10)** и **отзывы** (50–1000 символов)
- **Лайки** к комментариям
- **Жалобы** на книги (дубль, несоответствие жанра и др.)
- **Приватность профиля** — скрыть закладки от других
- **Обратная связь** администратору (мне нравится / не нравится / баг / идея)
- Чтение через **стандартный PDF-просмотрщик** (позиция чтения сохраняется автоматически)

### 👨‍💼 Для администраторов:
- Добавление/редактирование/удаление книг (с загрузкой обложки и PDF)
- Модерация жалоб
- Бан пользователей с указанием **срока** и **причины**
- Отправка **системных уведомлений** (например, о технических работах)
- Управление пользователями (поиск, сортировка, фильтрация)

### 👑 Для суперадминистраторов:
- Назначение ролей **администратора** другим пользователям

---

## 🔒 Безопасность

- Полностью **локальное хранение данных** — нет зависимости от облака
- **Хеширование паролей** через **BCrypt**
- **Валидация и фильтрация** загружаемых файлов (только PDF, JPG/PNG; ограничение по размеру)
- **Защита от SQL-инъекций** (Entity Framework + параметризованные запросы)
- **Разграничение прав доступа** на уровне кода

---

## 💻 Требования и инструкции по запуску

Проект поставляется в **трёх вариантах**. Выберите подходящий в зависимости от вашей роли.

---

### 🖥️ 1. Для конечного пользователя — `BiblioLand_v1.0.2.zip` / `.rar`

**Что это:** Готовое к запуску автономное приложение.  
**Для кого:** Все, кто хочет просто пользоваться библиотекой — без разработки, настройки и установки СУБД.

#### 🔧 Требования:
- Windows 10 или 11  
- .NET Framework 4.8 (установлен по умолчанию в большинстве версий Windows)

> 💡 **SQL Server устанавливать не нужно!** Приложение использует встроенный **SQL Server Express LocalDB**.

#### 🚀 Инструкция:
1. Скачайте `BiblioLand.zip` или `BiblioLand.rar`.
2. Распакуйте архив.
3. Запустите `BiblioLand.exe`.

> ✅ Всё уже на месте: база данных `.mdf`, обложки, PDF-файлы — работать можно сразу.

---

### 💻 2. Для разработчиков — `BiblioLand_LocalDB_SourceCode_v1.0.2.zip`

**Что это:** Исходный код + локальная БД `.mdf`.  
**Для кого:** Разработчики, которые хотят запустить проект «как есть» без настройки сервера.

#### 🔧 Требования:
- Windows 10 или 11  
- Visual Studio 2019 или новее  
- .NET Framework 4.8 Developer Pack

> 💡 SQL Server не требуется — используется `.mdf` + LocalDB.

#### 🚀 Инструкция:
1. Распакуйте архив.
2. Откройте `BiblioLand.sln` в Visual Studio.
3. Соберите проект (**Release** или **Debug**).
4. В папку сборки (`bin/Debug` или `bin/Release`) скопируйте:
   - Файл `Biblio.mdf` из `Docs/БД/Biblio.mdf`
   - Папку `BiblioLandRes` из `Docs/BiblioLandRes`
5. Запустите приложение.

> 💡 **Совет по работе с локальной базой данных**  
> Чтобы выполнить SQL-запрос к файлу `.mdf` (например, для отладки или проверки данных), перейдите в Visual Studio:  
> **Вид → Обозреватель объектов SQL Server**.  
> В открывшемся окне найдите свою базу данных (обычно: `(localdb)\MSSQLLocalDB` → **Databases** → `Biblio.mdf`),  
> кликните по ней правой кнопкой мыши и выберите **«Создать запрос»**.  
> Это откроет редактор запросов, подключённый к вашей локальной БД.

---

### 🔧 3. Для опытных разработчиков — `BiblioLand_SourceCode_v1.0.2.zip`

**Что это:** Только исходный код. БД нужно создать и настроить вручную.  
**Для кого:** Разработчики, которые хотят использовать собственный SQL Server (SSMS).

#### 🔧 Требования:
- Windows 10 или 11  
- Visual Studio 2019+  
- .NET Framework 4.8  
- **SQL Server 2018+**  
- **SQL Server Management Studio (SSMS)**

#### 🚀 Инструкция:
1. **Настройте базу данных:**
   - Откройте SSMS.
   - Создайте новую базу данных (например, `BiblioLand`).
   - Выполните скрипт: `Docs/БД/script.sql`.
   - Создайте триггеры: `Docs/БД/Тригеры.txt`.
   - При необходимости настройте задания (агенты): см. `Docs/БД/Агент.docx`.

2. **Настройте проект:**
   - Откройте `BiblioLand.sln`.
   - **Вариант A (проще):** Отредактируйте `App.config`:
     ```xml
     <connectionStrings>
         <add name="BiblioModel"
              connectionString="data source=ВАШ_СЕРВЕР;initial catalog=BiblioLand;integrated security=True;..."
              providerName="System.Data.SqlClient" />
     </connectionStrings>
     ```
     Замените `ВАШ_СЕРВЕР` на имя вашего экземпляра (например, `.\SQLEXPRESS` или `localhost`).
   - **Вариант B (гибче):** Если вы изменили структуру БД — следуйте инструкции в `Docs/БД/Модели.docx` для обновления моделей Entity Framework.

3. **Добавьте ресурсы:**
   - После сборки (`bin/Debug` или `bin/Release`) скопируйте папку `BiblioLandRes` из `Docs/` и вставьте рядом с `BiblioLand.exe` в соответветствующей папке внутри каталога `bin/`.

4. Запустите приложение.

> 💡 Если вы не меняли структуру БД — достаточно изменить только `data source` в `App.config`.

---

## 📁 Структура проекта

- **19 пользовательских форм**  
- **9 административных форм**  
- **13 кастомных контролов** (для переиспользования: карточки книг, отзывы, элементы закладок и т.д.)  
- **22 класса** (включая сервисы, валидаторы, хелперы)  
- Полностью автономная работа

---

## ⚠️ Возможные проблемы и решения

### 1. Приложение не запускается

В редких случаях после закрытия приложения его процесс может остаться в памяти и продолжать работать в фоне. Это приводит к тому, что при повторной попытке запуска приложение не открывается.

**Решение:**
1. Откройте **Диспетчер задач** (`Ctrl + Shift + Esc`).
2. Перейдите на вкладку **«Процессы»**.
3. Найдите процесс **`BiblioLand.exe`**.
4. Кликните по нему правой кнопкой мыши и выберите **«Снять задачу»**.
5. После этого запустите приложение снова — оно должно работать корректно.

> 💡 Такое поведение может возникать из-за особенностей завершения работы WinForms-приложений и не связано с ошибками в коде.

---

### 2. Ошибка: «Не удалось обработать файл, так как он содержит веб-метку»

При первом запуске версии для разработчиков (особенно после распаковки архива, скачанного из интернета) Windows может блокировать выполнение файлов из ненадёжных источников. Вы увидите сообщение вида:

> *«Не удалось обработать файл ...»*  

Это вызвано наличием **веб-метки (mark of the web)** — специальной метки NTFS, которую Windows добавляет к файлам, загруженным из интернета.

**Решение:**
1. Найдите файл(ы), указанные в сообщении об ошибке (обычно это `.resx`).
2. Кликните по файлу **правой кнопкой мыши** → **«Свойства»**.
3. Внизу окна вы увидите надпись:  
   > *«Этот файл получен с другого компьютера ...»*
4. Поставьте галочку **«Разблокировать»**.
5. Нажмите **«Применить»** → **«ОК»**.
6. Повторите для всех остальных заблокированных файлов (если нужно).
7. Запустите приложение снова.

> 💡 Чтобы избежать этой проблемы в будущем, можно **разблокировать весь архив до распаковки** — тогда все извлечённые файлы будут доверенными.

--- 

## ⭐ Поддержка проекта

Если вам понравился BiblioLand — поставьте ⭐ этому репозиторию!  

---

## ⚖️ Лицензия

Проект распространяется по лицензии **MIT**.  
См. файл [LICENSE](./LICENSE).

---

> 💬 *BiblioLand — это демонстрация того, что мощные, безопасные и многофункциональные приложения можно создавать и в локальной среде, без зависимости от глобальных сетей.*
